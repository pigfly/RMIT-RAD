default: &default
  adapter: postgresql
  encoding: unicode
  # `host.docker.internal` returns the IP address of the host machine
  host: host.docker.internal
  port: 54321
  username: alex
  password:
  pool: 5

development:
  <<: *default
  database: myapp_development


test:
  <<: *default
  database: myapp_test